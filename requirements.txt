# Runtime dependencies for replayd.
# Used as input to flatpak-pip-generator to produce the pip sources block
# in the Flatpak manifest.
#
# PyQt6 and GStreamer bindings come from the Freedesktop SDK and are NOT listed
# here -- they are available at runtime through the org.freedesktop.Platform SDK.
#
# Generate manifest pip sources:
#   python3 flatpak-pip-generator --runtime=org.freedesktop.Platform//24.08 \
#       dbus-next pulsectl qasync

dbus-next>=0.2.3
pulsectl>=23.5.2
qasync>=0.27.1
